<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>Url shortner</title>
  </head>
  <body>
    <h1>My first url shortener</h1>

    <form action="/" method="post">
      <input type="url" name="url" id="url" placeholder="url here..." />
      <input type="submit" value="Shrink" />
    </form>

    <div class="short-url">
      <% if (locals.short_url) { %>
       <div class="success"> success ! <br>Here is your link :
      <a href="<%= short_url %>">
        <%= short_url %> </a></div>
      <% } %>
      <% if (locals.error) { %>
        <p class="error"><%= error %></p>
        <% }%>
    </div>
  </body>
</html>
